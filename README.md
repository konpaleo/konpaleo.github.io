# Personal portfolio

This is a starter Jekyll site for `konpaleo.github.io`. It is intentionally
small and easy to customize.

## Run locally

Install Ruby and Bundler, then run:

```bash
bundle install
bundle exec jekyll serve --livereload
```

Open <http://localhost:4000> in your browser.

## Customize

1. Change your name, email, and site description in `_config.yml`.
2. Replace the sample projects in `_data/projects.yml`.
3. Add project images to `assets/images/` and update each project's `image`.
4. Replace the placeholder copy in `index.md` and `about.md`.

The `_site/` directory is generated by Jekyll and is intentionally ignored by
Git. GitHub Pages builds the site from the repository source automatically.
